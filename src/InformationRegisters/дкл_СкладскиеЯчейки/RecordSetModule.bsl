
Процедура ПередЗаписью(Отказ, Замещение)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого Запись Из ЭтотОбъект Цикл
		Если ЗначениеЗаполнено(Запись.depth) И ЗначениеЗаполнено(Запись.width) 
			И ЗначениеЗаполнено(Запись.poseX) И ЗначениеЗаполнено(Запись.poseY) Тогда
			Запись.ПараметрыЗаполнены = Истина;
		Иначе
			Запись.ПараметрыЗаполнены = Ложь;
		КонецЕсли;
	КонецЦикла;                     
	
КонецПроцедуры
